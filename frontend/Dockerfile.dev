FROM node:24-alpine
RUN corepack enable && corepack prepare pnpm@11.24.0 --activate
WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

ENV NEXT_TELEMETRY_DISABLED=1 PORT=3001
EXPOSE 3001
CMD ["pnpm", "dev"]
