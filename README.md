# accessibility


Para arrancar el proyecto lo primero que hay que hacer es ejecutar el comando
docker compose up --build

Si ya lo tienes buildeado puedese ejecutar directamente este comando
docker compose up --watch
De esta forma si actualizas cosas se actualizara automaticamente

TODO: Hacer un makefile para poder facilitarme la ejecucion de comandos