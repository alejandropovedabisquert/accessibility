services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/scan-results:/app/scan-results
    develop:
      watch: 
        - action: sync
          path: "./backend"
          target: "/app"
          ignore:
            - "node_modules"
        - action: rebuild
          path: "./backend/package.json"